% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simloglm.R
\name{simloglm}
\alias{simloglm}
\title{Simulate from a linear regression model with a logged dependent variable}
\usage{
simloglm(
  input_obj,
  nsim_est = 1000,
  nsim_fund = 1000,
  scenario = NULL,
  X = NULL,
  multiplier = NULL,
  observed_value_approach = FALSE,
  logged_dv = TRUE,
  predicted_values = FALSE,
  fast = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{input_obj}{Either an object of class "lm" (the output from a call to
lm) or a user provided list with the entries beta_hat (the estimated
regression coefficients), unscaled_vcov (the unscaled variance covariance
matrix), sigma_hat (the estimated residual standard error), n (the number of
observations) and k (the number of regression coefficients). The list can be
provided for more flexibility. Most users will call simloglm directly on the
output from a call to lm.}

\item{nsim_est}{Number of simulations to simulate estimation uncertainty
(defaults to 1000).}

\item{nsim_fund}{Number of simulations to simulate fundamental uncertainty
(defaults to 1000). Only used when \code{fast = FALSE}.}

\item{scenario}{Named list with values (scalar or vector; vectors must all
have the same length, scalars are recycled) for the independent variables.
The names must match the \emph{variables} of the regression model, and the values
are given on the \strong{original scale}: for \code{log(dist) ~ log(speed)} you supply
\code{speed}, not \code{log(speed)}, and the \code{log()} is applied for you. Variables you
leave out are held at their mean (numeric) or reference level (factor).
Defaults to \code{NULL}, which holds every variable at its mean or reference
level.}

\item{X}{Sometimes it is easier to pass the scenario directly as a model
matrix (e.g. when you want to set factors to their observed proportions).
This overrides \code{scenario}. In contrast to \code{scenario}, the columns of \code{X}
must already be \strong{transformed}: they are used as the rows of the model
matrix exactly as supplied, in the order of the coefficients. (Default is
NULL.)}

\item{multiplier}{Named list of multiplicative shifts, e.g.
\code{list(income = c(1, 1.1))} for the observed sample and a ten percent
increase. Only available together with \code{observed_value_approach = TRUE},
and mutually exclusive with \code{scenario}. A proportional shift is the natural
counterfactual under an elasticity, where setting everybody to the same
value is not. (Default is NULL.)}

\item{observed_value_approach}{Do you want to use the observed value
approach? Instead of predicting for one synthetic average case, the
counterfactual is applied to every observation in the estimation sample and
the predictions are averaged. (Default is FALSE.)}

\item{logged_dv}{Is the dependent variable in the linear model logged?
Defaults to \code{TRUE}, the premise of this package. Set it to \code{FALSE} to
simulate on the scale of the dependent variable, without the exponentiation
and without the sigma^2 / 2 correction. Pass \code{NULL} to have it guessed from
the model formula -- note that this is only a guess, because a dependent
variable that was logged before the model was fitted (\code{logdist ~ .}) is
indistinguishable from one that was not.}

\item{predicted_values}{Do you also want to output predicted values? (Default
is FALSE. The resulting object can be very big.) Requires \code{fast = FALSE}.}

\item{fast}{Do you want to speed up computation by not explicitly simulating
predicted values? The fast path evaluates the same quantities in closed form
and should be preferred for point estimates. (Default is TRUE.)}

\item{verbose}{Print progress bars and informational messages? (Default is
TRUE.)}
}
\value{
A list of class "simloglm" with the entries \code{median} and \code{mean}: two
matrices with \code{nsim_est} rows and one column per scenario, holding the
simulated conditional geometric mean (equivalently, the median) and the
conditional arithmetic mean of the dependent variable. \code{median_point_estimate}
and \code{mean_point_estimate} hold the corresponding quantities evaluated at the
coefficient estimates, and \code{predicted_values} a list of predicted value
matrices if these were requested.
}
\description{
Simulate from a linear regression model with a logged dependent variable
}
\examples{
df <- cars
regression <- lm(log(dist) ~ speed, data = df)

# Specifying no scenario simulates at the mean of speed.
at_the_mean <- simloglm(regression, verbose = FALSE)
get_summary(at_the_mean)

# Explicitly specifying a scenario.
res <- simloglm(regression, scenario = list(speed = c(5, 10, 20)),
                verbose = FALSE)
get_summary(res)

# Scenario values are given on the original scale, also in a log-log model.
loglog <- lm(log(dist) ~ log(speed), data = df)
res_loglog <- simloglm(loglog, scenario = list(speed = c(10, 11)),
                       verbose = FALSE)
get_ratio(res_loglog)
}
